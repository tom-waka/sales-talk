<% if logged_in? %>
  <% if (current_user?(@user) || current_user.admin?) && !current_user.test_user? %>
    <p><%= link_to "登録情報を編集する", edit_user_path(@user), class: "btn" %></p>
  <% end %>
<% end %>