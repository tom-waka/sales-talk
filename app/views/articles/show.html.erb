<% provide(:title, "記事詳細") %>

<h1>記事詳細</h1>
<p><%= "#{@article.user.name}の投稿" %></p>
<p><%= @article.title %></p>
<p><%= simple_format(h(@article.content), {}, sanitize: false, wrapper_tag: "div") %></p>
<p><%= @article.created_at.to_s(:datetime) %></p>
  <% if logged_in? %>
    <% if current_user.admin? || is_mine?(@article)  %>
      <%= link_to "編集", edit_article_path(@article) %>
      <%= link_to "削除", article_path(@article), method: :delete, data: {confirm: "記事を削除します。よろしいですか？"} %>
    <% end %>
  <% end %>